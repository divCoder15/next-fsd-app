import { appName } from '@/shared/const/system';

import { icons } from './pagesIcons';

export const pagesMetadata = {
  home: {
    icons,
    title: `Home | ${appName}`,
    description: '',
  },
};
